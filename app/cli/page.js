import CliPage from '@/components/cli/cli-page';

export const metadata = {
  title: 'Vaulter CLI | Setup and Commands',
  description: 'Install the Vaulter CLI, authenticate in the browser, and run common vault workflows from your terminal.'
};

export default function Page() {
  return <CliPage />;
}
